salario_por_hora = float(input("Quanto você ganha por hora? "))
horas_trabalhadas = float(input("Quantas horas trabalhou no mês? "))
print(f"Salário mensal: R${(salario_por_hora*horas_trabalhadas):.2f} .")
