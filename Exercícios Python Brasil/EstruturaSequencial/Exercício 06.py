# Dica: Area do circulo é pi*raio*raio
# Para obter o número pi, utilizaremos a constante definida na biblioteca math
from math import pi
raio = float(input("Raio: "))
print(f"Área: {(pi*raio**2):.2f} .")
